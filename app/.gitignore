# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Updater signing keys — NEVER commit these
.tauri-keys/
*.key
*.key.pub

# Local dev env (machine-specific server paths)
dev.env
run-dev.sh

# Bundled sidecar binary + model files (large, built/copied locally)
src-tauri/binaries/
src-tauri/models/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Bundled onedir server (large, built locally)
src-tauri/server-dist/
